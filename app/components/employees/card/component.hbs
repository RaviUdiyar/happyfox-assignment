<div
  class="employee-card p-4 border border-grey"
  draggable="{{unless @employee.isCEO true false}}"
>
  <div class="employee-card-image border p-2">

  </div>
  <div class="flex flex-col">
    <span class="font-bold">{{@employee.name}}</span>
    <p>{{@employee.designation}}</p>
  </div>
</div>
